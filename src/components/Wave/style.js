import { StyleSheet } from 'react-native';

const waveStyles = StyleSheet.create({
  headSection: {
    width: '100%',
    height: '58%',
  },
  waveDivider: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    width: '100%',
    overflow: 'hidden',
    lineHeight: 0,
  },
});

export default waveStyles;
